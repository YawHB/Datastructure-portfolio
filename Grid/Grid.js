export class Grid {
    constructor(rows, cols) {
        this.rows = rows;
        this.cols = cols;
        //this.array = [rows][cols];
        let array = [];
        for (let row = rows; row < rows; row++) {
            array.push('');
        }
    }
}
